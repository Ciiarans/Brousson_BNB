<nav data-controller="navbar" data-navbar-target="navbar" class="navbar navbar-expand-sm navbar-light py-3 navbar-sticky">
  <div class="container d-flex justify-content-between align-items-center h-100">

    <!-- Conteneur central avec liens et logo -->
    <div class="d-flex flex-grow-1 justify-content-center align-items-center">
      <!-- Lien vers les appartements -->
      <%= link_to "#{root_path}#locations", class: "text-decoration-none d-flex align-items-center" do %>
        <p class="mb-0">Nos logements</p>
      <% end %>

      <!-- Logo au centre avec marge autour -->
      <%= link_to root_path, class: "text-decoration-none d-flex align-items-center mx-5" do %>
        <%= cl_image_tag '', alt: "Logo Immobilier", class: "mb-0", data: { navbar_target: "logo" } %>
      <% end %>

      <!-- Lien vers formulaire de contact -->
      <%= link_to "#{root_path}#form-contact", class: "text-decoration-none d-flex align-items-center" do %>
        <p class="mb-0">Nous contacter</p>
      <% end %>
    </div>

    <!-- Espaceur pour aligner le bouton à droite -->
    <div class="dropdown d-flex align-items-center">
      <% if user_signed_in? %>
        <% if current_user.email == ENV['SITE_KEY'] %>
          <button class="btn btn-primary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
            <%= "Bonjour Pat'" %>
          </button>
          <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButton1">
            <li>
              <%= link_to "Tableau de bord", profile_path, class: "dropdown-item text-decoration-none" %>
            </li>
            <li>
              <%= link_to "Déconnexion", destroy_user_session_path, method: :delete, class: "dropdown-item text-danger" %>
            </li>
          </ul>
        <% end %>
      <% end %>
    </div>

  </div>
</nav>
